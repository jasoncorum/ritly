<% if @link.errors.any? %>
	<% @link.errors.full_messages.each do |msg| %>
	<li><%= msg %></li>
	<% end %>
<% end %>


<%= form_for @link do |f| %>
	<div>
		<%= f.label :url, "Please enter a URL:" %><br />
		<%= f.url_field :url %>
	</div>
	<%= f.submit "Create URL" %>
<% end %>